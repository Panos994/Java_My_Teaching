package trainssystem.regionaltrains;

public class LocalTrains extends RegionalTrain{
    public LocalTrains(int trainNumber, boolean hasWifi, double maxSpeed, String color, int km) {
        super(trainNumber, hasWifi, maxSpeed, color, km);
    }
}
