package trainssystem.regionaltrains;

public class DistrictTrains extends RegionalTrain{ //επαρχιακα τρενα

    public DistrictTrains(int trainNumber, boolean hasWifi, double maxSpeed, String color, int km) {
        super(trainNumber, hasWifi, maxSpeed, color, km);
    }
}
