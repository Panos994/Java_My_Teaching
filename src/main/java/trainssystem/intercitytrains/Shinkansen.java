package trainssystem.intercitytrains;

public class Shinkansen extends IntercityTrain{
    protected Shinkansen(int trainNumber, String seatClass, double maxSpeed, String color, int km) {
        super(trainNumber, seatClass, maxSpeed, color, km);
    }
}
