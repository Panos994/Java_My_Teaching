package trainssystem.intercitytrains;

public class EuroCity extends IntercityTrain{
    public EuroCity(int trainNumber, String seatClass, double maxSpeed, String color, int km) {
        super(trainNumber, seatClass, maxSpeed, color, km);
    }

    @Override
    public void printIntercityTrain() {
        super.printIntercityTrain();
    }
}
