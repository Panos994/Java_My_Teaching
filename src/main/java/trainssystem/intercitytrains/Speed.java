package trainssystem.intercitytrains;

public class Speed extends IntercityTrain{
    protected Speed(int trainNumber, String seatClass, double maxSpeed, String color, int km) {
        super(trainNumber, seatClass, maxSpeed, color, km);
    }
}
