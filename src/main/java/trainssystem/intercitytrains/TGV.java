package trainssystem.intercitytrains;

public class TGV extends IntercityTrain{

    protected TGV(int trainNumber, String seatClass, double maxSpeed, String color, int km) {
        super(trainNumber, seatClass, maxSpeed, color, km);
    }
}
