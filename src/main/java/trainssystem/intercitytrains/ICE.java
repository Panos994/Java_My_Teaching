package trainssystem.intercitytrains;

public class ICE extends IntercityTrain{

    protected ICE(int trainNumber, String seatClass, double maxSpeed, String color, int km) {
        super(trainNumber, seatClass, maxSpeed, color, km);
    }
}
